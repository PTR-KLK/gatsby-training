import styled from "@emotion/styled"
import { Link } from "gatsby"

export const PageLink = styled(Link)`

    color: inherit;

    &:visited {
        color: inherit;
    }

    &:hover {
        color: unset;
    }
`;